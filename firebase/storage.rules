rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 認証済みユーザーのみアクセス可（Phase 3以降でパスベースのルールを追加）
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
