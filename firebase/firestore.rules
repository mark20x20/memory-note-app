rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Phase 1 で実装する
    // 現時点では全拒否（安全デフォルト）
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
