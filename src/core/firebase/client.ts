// Firebase クライアント初期化
// Phase 1 で実装する
// 必要な環境変数: EXPO_PUBLIC_FIREBASE_* (.env.example 参照)

export const firebaseApp = null; // Phase 1: initializeApp(firebaseConfig)
export const db = null;          // Phase 1: getFirestore(firebaseApp)
export const auth = null;        // Phase 1: getAuth(firebaseApp)
export const storage = null;     // Phase 1: getStorage(firebaseApp)
export const functions = null;   // Phase 1: getFunctions(firebaseApp)
